#include "main.h"

/**
 * mul - multiplies two integers a and b.
 * @a: first number.
 * @b: second number.
 * Return:returns multiplication of a and b.
 */
int mul(int a, int b)
{
	return (a * b);
}
